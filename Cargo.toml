[package]
name = "generate-event"
version = "0.1.0"
authors = ["Diego Rubin <rubin.diego@gmail.com>"]
edition = "2018"

[dependencies]
libloading = { version = "0.6.2" }
amiquip = { version = "0.3", default-features = false }
rdkafka = { version = "0.23", features = ["cmake-build"] }
